scalar GraphQLDateTime

type User {
  id: ID!
  name: String!  
}
type Project {
    id: ID!
    name: String!
    description: String
    active: Boolean!
    count: Int!
    goals: [Goal!]
    wordcounts: [WordCount!]
}
type Goal {
    id: ID!
    active: Boolean!
    start: GraphQLDateTime!
    end: GraphQLDateTime!
    count: Int!
}
type WordCount {
    id: ID!
    created: GraphQLDateTime!
    count: Int!
}